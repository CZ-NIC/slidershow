{
    "name": "slidershow",
    "private": true,
    "description": "Dev-only dependencies: editor IntelliSense + type check (jsconfig.json) and Playwright tests. The app itself has no build step and loads runtime dependencies from CDN (see slidershow/slidershow.js).",
    "scripts": {
        "test": "playwright test",
        "typecheck": "tsc -p jsconfig.json"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/CZ-NIC/slidershow.git"
    },
    "devDependencies": {
        "@playwright/test": "^1.61.1",
        "@types/jquery": "^4.0.1",
        "@types/jqueryui": "^1.12.24",
        "@types/leaflet": "^1.9.21",
        "typescript": "^6.0.3"
    }
}
